/** @type {import('next').NextConfig} */
const nextConfig = {
  // Env vars are read directly from process.env at runtime on Vercel.
  // No need to duplicate here — Vercel injects them from the dashboard.
};

export default nextConfig;
